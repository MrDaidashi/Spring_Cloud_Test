spring:
  cloud:
    config:
      name: service-feign, eureka-client
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
#      uri: http://localhost:8888/

feign:
  hystrix:
    enabled: true
